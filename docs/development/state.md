# cyim — State Snapshot

> **Volatile.** This file is the live state of the project — current version, sizes, in-flight slot, consumer build status. Bumped every release (ideally by the release post-hook). Don't put durable rules here — those live in [`CLAUDE.md`](../../CLAUDE.md).

*Last bumped: 2026-05-09 (v1.6.1 — **Toolchain + vyakarana catch-up cut**. First bite of the 1.6.x catch-up channel. `cyrius` pin 5.9.16 → 5.10.10 (cyim + cyim-lsp lockstep — cyim-lsp tag stays 1.2.1, only its `[package].cyrius` moved). `vyakarana` tag 1.0.2 → 2.2.1, skipping 14 minor cuts. The only breaking surface across that window is vyakarana 2.0.0's `tokenize_source` → streaming-primitive replacement; one call site in `src/highlight.cyr`, mechanical 5-line patch + null-handle guard preserves the 1.x null-on-unregistered-grammar contract. 35 new vyakarana grammars are linked into the binary but `src/lang.cyr` extension routing still covers only the original 10 — full grammar wiring is the 1.6.2 bite. Bench: cold tokenize regresses as expected from 2.0's per-call alloc overhead (`highlight_buf_1MB_cyrius` 253ms → 318ms, +25%; `highlight_buf_cache_hit_x1000` 16µs → 17.5µs, +10% — within sampling noise on the hot interactive path). Binary 1,203,936 B (+238,504 over 1.6.0; vyakarana 2.2.1's 45 bundled grammars dominate the delta). All gates: 22 suites / 1047 assertions / 3 fuzz / lint clean / DCE build clean. ABI freeze unchanged.)*

*Prior bump: 2026-05-07 (v1.6.0 — **VIM-style marks**. First feature minor of the post-1.5.x cycle. `m<letter>` set + `'<letter>` jump, per-buffer (a-z) + global (A-Z) namespaces. ~150 LOC in new `src/marks.cyr` + ~80 LOC in mode.cyr prefix dispatch. New `tests/marks.tcyr` (50 assertions across 19 groups). Fuzz harness updated; surfaced + fixed a NULL-buf-ptr corner case in marks_get_global (sentinel changed from `offset == -1` to `buf_ptr != 0`). 21 test suites, all gates green. Binary 965,432 B (+7,712 over 1.5.3). No plugin ABI changes; freeze still holds.)*

*Prior bump: 2026-05-07 (v1.5.3 — **closes 3 of 4 LOW closeout findings**. F-CO-1 (multi-iter bench, no real render_build_line regression — was 1-iter noise), F-CO-3 (defensive prefix-clear in `plugin_list_display`), F-CO-4 (URL-decode for `file://` URIs via cyim-lsp 1.2.1's new `lsp_uri_decode`). F-CO-2 stays informational. Plugin tests 85 → 88 (+3); bundle 2228 → 2305 lines (cyim-lsp 1.2.1 source change). Binary 957,720 B (+1,352 over 1.5.2). Reference previews / open-in-split / arrow keys remain deferred LSP polish; 1.6.0 may now open.)*

*Prior bump: 2026-05-07 (v1.5.2 — **closeout cut for the 1.5.x cycle**. Pure verification per CLAUDE.md "Closeout Pass" policy. All 11 audit steps PASS; 4 LOW findings (F-CO-1 perf noise, F-CO-2 refactor wait-for-third, F-CO-3 prefix-clear hardening, F-CO-4 URL-decode) tracked in roadmap as 1.5.x patches before 1.6.0. No CRITICAL / HIGH / MEDIUM. Roadmap rewritten — closed milestones trimmed, deferred work organized. Two stale comments in `src/plugin.cyr` updated. Binary byte-identical to 1.5.1 modulo `_VERSION_STR_CYIM`. Audit: [`docs/audit/2026-05-07-1.5x-closeout.md`](../audit/2026-05-07-1.5x-closeout.md).)*

*Prior bump: 2026-05-07 (v1.5.1 — **cyim-lsp 1.2.0 pickup**. `:lsp-find-refs` / `gr` becomes a navigable quickfix picker — last deferred LSP UI gap closes and last named consumer of cyim 1.5.0's `plugin_list_display` ABI. Updated `src/plugins/lsp_glue.cyr` mirrors cyim-lsp 1.2.0's reference glue: refs response parser + filename:line:col label builder + on_select that loads via plugin_buf_load_file and jumps. Bundle source unchanged at cyim-lsp 1.2.0; banner-only regen. Binary 956,368 B (+2,856 over 1.5.0). All gates green.)*

*Prior bump: 2026-05-07 (v1.5.0 — **`plugin_list_display` ABI**. Bottom-anchored popup picker; closes the third deferred ABI from cyim 1.4.2's ADR 0004 amendment. j/k/Enter/Esc/q surface; reverse-video highlight; auto-scrolling window keeps selection visible. Single active list, replaceable. plugin.tcyr 58 → 85 assertions across 12 new groups. Additive — cyim 1.x ABI freeze holds. cyim-lsp 1.1.x or 1.2.0 next: activates `:lsp-find-refs` as a navigable quickfix using `plugin_list_display` + `plugin_buf_load_file`.)*

*Prior bump: 2026-05-07 (v1.4.3 — **cyim-lsp 1.1.0 pickup**. `gd` / `gr` keymaps + cross-file goto-def now activate via cyim 1.4.2's ABI surfaces. `[deps.cyim-lsp].tag` 1.0.3 → 1.1.0; `src/plugins/lsp_glue.cyr` adds `_cyim_lsp_gd`/`_cyim_lsp_gr` handlers + cross-file load via `plugin_buf_load_file` + prefix-key registrations. Bundle source unchanged at 1.1.0 (banner-only regen). Reference quickfix list + URL-decoded paths still deferred. All gates green.)*

*Prior bump: 2026-05-07 (v1.4.2 — **plugin ABI extensions**. Two new public surfaces (`plugin_register_normal_prefix_key`, `plugin_buf_load_file`) + the long-stubbed `gg` motion finally wired (ACT_MOVE_FILE_START + KEY_G prefix). Additive per ADR 0004 — no breaking changes. plugin.tcyr extended 33 → 58 assertions. cyim-lsp 1.1.0 will consume both new surfaces to activate `gd`/`gr` keymap dispatch + cross-file goto-def. The list-display ABI (refs quickfix) deferred to cyim 1.5.0 — popup-overlay subsystem warrants a minor.)*

---

## Version

- **VERSION**: `1.6.1`
- **Cyrius toolchain pin**: `5.10.10` (was 5.9.16 — bumped in 1.6.1 alongside vyakarana 1.0.2 → 2.2.1)
- **Last release**: `1.6.1` — Patch. Toolchain + vyakarana catch-up. cyrius pin 5.9.16 → 5.10.10; vyakarana tag 1.0.2 → 2.2.1; cyim-lsp's own pin moves to 5.10.10 (no tag bump). Single breaking surface in vyakarana 1.x → 2.x is `tokenize_source` removal; `src/highlight.cyr` migrates to `tokenize_stream_new` + `_feed` + `_finish` + `_free` with `if (s == 0) { return 0; }` guard preserving 1.x's null-on-unregistered-grammar contract. Header comments at `:9` and `:21` rewritten to describe the streaming shape. 35 new vyakarana grammars are bundled in the distfile but `src/lang.cyr` extension routing still covers only the original 10 — full grammar wiring is the 1.6.2 bite. Bench: `highlight_buf_1MB_cyrius` 253ms → 318ms (+25%, avg of 3 runs) — vyakarana 2.0's per-call alloc overhead, expected per its CHANGELOG; `highlight_buf_cache_hit_x1000` 16µs → 17.5µs (+10%, within sampling noise on the hot interactive path). Binary 1,203,936 B (+238,504 over 1.6.0; vyakarana 2.2.1's 45 bundled grammars dominate the delta). 22 test suites / 1047 assertions / 3 fuzz / lint 0 warnings / DCE build clean. ABI freeze unchanged. 2026-05-09
- **Prior release**: `1.6.0` — Minor: VIM-style marks. `m<letter>` sets a mark at the cursor (per-buffer a-z stored as `vec<{buf_ptr, key, offset}>`; global A-Z stored as a 26 × 16 B static array indexed by `key - 'A'`). `'<letter>` jumps to it (clamped to `buf_len` defensively for post-delete drift safety; switches active buffer for global marks). Two new prefix constants `KEY_M` (109) / `KEY_QUOTE` (39) join `KEY_CTRL_W` / `KEY_G` in mode.cyr's generalized prefix dispatch. New `bl_find_by_buf(s, b)` helper. Sentinel for "unset" global is `buf_ptr == 0` (default-zero static state is therefore valid, no marks_init prerequisite for safety). Fuzz harness updated; surfaced + fixed a NULL-buf-ptr edge in marks_get_global. tests/marks.tcyr 50 assertions across 19 groups; total cyim test surface 88+50 = ~1023 assertions across 21 suites. Plugin ABI freeze (1.3.6 / ADR 0004) holds — marks are mode-internal, not exposed to plugins. Binary 965,432 B (+7,712 over 1.5.3). 2026-05-07
- **Prior release**: `1.5.3` — Closes 3 of 4 LOW closeout findings. **F-CO-1**: `tests/perf.bcyr` converted to multi-iter sampling for noise-prone short-runtime benches (10 outer rounds × 1000 inner for render_build_line + cache_hit; 3 outer rounds for search/move). Verdict: the +18% render_build_line "regression" at 1.5.1 was 1-iter sampling noise; 250μs avg ± 4μs across 10 iters. **F-CO-3**: `plugin_list_display` now `editor_set_prefix(s, 0)`s defensively at entry — closes the unreachable-today corner case where a latched KEY_G prefix would survive a synchronously-displayed picker. +3 assertions in `tests/plugin.tcyr`. **F-CO-4**: cyim-lsp 1.2.1's new public `lsp_uri_decode(uri)` (real bundle source change at the cyim-lsp side, +77 lines for `_lsp_hex_digit` + decoder) consumed in `src/plugins/lsp_glue.cyr` cross-file branches — files with spaces / non-ASCII / `%XX` escapes in `file://` URIs now load correctly via cross-file goto-def + refs quickfix. F-CO-2 stays informational. Binary 957,720 B (+1,352 over 1.5.2 — F-CO-3 ~80 B + F-CO-4 call sites ~400 B + cyim-lsp 1.2.1 helpers linked in ~870 B). 1.6.0 may now open. 2026-05-07
- **Prior release**: `1.5.2` — Closeout cut for the 1.5.x cycle. Pure verification per CLAUDE.md "Closeout Pass" policy ("Run before tagging X.Y.0 ... Ship as the last patch of the current minor"). All 11 audit steps PASS; binary byte-identical to 1.5.1 modulo the regenerated `_VERSION_STR_CYIM` string (pure verification cut — no runtime code changes; only stale-comment edits in `src/plugin.cyr`). 4 LOW findings tracked: F-CO-1 (render_build_line +18% 1-iter measurement noise — re-bench multi-iter), F-CO-2 (refactor "load uri + jump to lc" pattern at 2 instances — defer to 3rd consumer), F-CO-3 (defensive prefix-clear when `plugin_list_display` is called — unreachable today, future hardening), F-CO-4 (URL-decode for `file://` URIs — UX gap, not security). All patch-sized; tracked in roadmap.md's 1.5.x cycle section as items to ship before 1.6.0. Audit: [`docs/audit/2026-05-07-1.5x-closeout.md`](../audit/2026-05-07-1.5x-closeout.md). 2026-05-07
- **Prior release**: `1.5.1` — cyim-lsp 1.2.0 pickup. The `plugin_list_display` ABI from 1.5.0 has its first real consumer: cyim-lsp 1.2.0's reference glue activates `:lsp-find-refs` / `gr` as a navigable quickfix popup. cyim's `src/plugins/lsp_glue.cyr` mirrors the change — drops the `_cyim_lsp_refs_msg` static buffer and the status-bar count format, adds `_cyim_lsp_parse_refs` + `_cyim_lsp_label_for_ref` + `_cyim_lsp_on_ref_select` + three module-level parallel payload vecs. User-visible: `gr` pops up a list of `filename:line:col` items; j/k/Enter navigates + jumps; Esc/q dismisses. Bundle source unchanged at cyim-lsp 1.2.0 (banner-only regen). Binary 956,368 B (+2,856 over 1.5.0). The full LSP user-visible surface (gd / gr / cross-file goto-def / refs quickfix / diagnostics) is now active. Deferred: URL-encoded URIs, reference previews, open-in-split. 2026-05-07
- **Prior release**: `1.5.0` — Minor. `plugin_list_display(s, items, count, on_select)` ABI ships — bottom-anchored popup picker drawn after buffer + status, j/k/Enter/Esc/q input surface, reverse-video highlight on current item, auto-scrolling window. Storage is module-global (single active list, replaceable). Dispatch interception sits at the very top of `editor_dispatch` returning ACT_NONE for all keys while active so motion/edit pipelines stay no-ops. `_plugin_list_select` dismisses BEFORE firing on_select so chained pickers work cleanly. tests/plugin.tcyr 58 → 85 assertions across 12 new groups. Additive ABI per ADR 0004's "additions allowed" envelope — no frozen-from-1.3.6 symbol changed shape. Binary 953,512 B (+3,064 over 1.4.3). cyim-lsp 1.1.x/1.2.0 will consume this for `:lsp-find-refs` / `gr` quickfix; arrow keys deferred (require editor_feed-side wiring); URL-decoded paths still deferred. 2026-05-07
- **Prior release**: `1.4.3` — cyim-lsp 1.1.0 pickup. The 1.4.2 ABI extensions (`plugin_register_normal_prefix_key`, `plugin_buf_load_file`) now have a live consumer: cyim-lsp 1.1.0's reference glue uses both, and cyim's `src/plugins/lsp_glue.cyr` mirrors the change. User-visible: `g` then `d` over a Cyrius symbol jumps to its definition (same-file: cursor moves; cross-file: file loads + cursor jumps in new buffer); `g` then `r` shows reference count. Built-in `gg` still wins on conflict for `(KEY_G, 'g')` per ADR 0003 §3 — verified at v1.4.2. Bundle source unchanged at cyim-lsp 1.1.0 (banner-only regen); cyim-lsp `[lib]` will only grow when something protocol-level lands. Binary 950,448 B (+560 over 1.4.2). Reference quickfix list (popup overlay) and URL-decoded paths in URIs still deferred — `plugin_list_display` ABI is the cyim 1.5.0 work. 2026-05-07
- **Prior release**: `1.4.2` — Plugin ABI extensions: (1) `plugin_register_normal_prefix_key(prefix, key, fp)` for two-byte NORMAL-mode sequences (e.g. cyim-lsp's `gd`/`gr`); (2) `plugin_buf_load_file(s, path)` for cross-file open + switch (cyim-lsp's cross-file goto-def). Both additive — `_plugin_normal_prefix_keys` vec stores 24 B `{prefix, key, fp}` records, plugin_buf_load_file thin-wraps the new `_buf_load_file_into_active` helper extracted from `:e <file>`. cyim's NORMAL-mode prefix dispatch generalized: previously special-cased Ctrl-W only; now handles any latched prefix and routes to built-in (Ctrl-W navigation, KEY_G `gg`) or plugin lookup. The long-stubbed `gg` motion is finally wired through ACT_MOVE_FILE_START. Built-ins win on conflict per ADR 0003 §3 — verified by test (registering plugin `(KEY_G, 'g', fp)` is shadowed by the built-in). Plugin prefix-keys can't drive mode transitions; that's an `ex_command` thing. tests/plugin.tcyr 33 → 58 assertions across 9 new groups. cyrius test 20 suites + fuzz 3 + smoke 1 all PASS. Binary 949,888 B (+1,872 over 1.4.1). cyim-lsp 1.1.0 next: activates the consumer side. 2026-05-07
- **Prior release**: `1.4.1` — Patch picking up cyim-lsp 1.0.3's subprocess env fix + first cyim `.smcyr` smoke harness. cyim-lsp 1.0.0–1.0.2 spawned cyrius-lsp with empty envp; child had no `PATH`, so `/usr/bin/env cyrius-lsp` (the default lazy-start lookup) silently failed in any real consumer environment. Surfaced by writing `tests/smcyr/lsp_fold.smcyr` (the "fold-in dry-run against a real consumer" the v1.0.0 freeze should have included per cyim-lsp ADR 0001 v1.0.2 amendment). cyim-lsp 1.0.3's `_lsp_proc_envp_from_self()` reads `/proc/self/environ` and populates execve's envp; cyim 1.4.1 picks it up via dep tag bump. Smoke now passes 13/13 assertions; the `[cyrius-lsp] initialized` line in stderr confirms the server received + responded. Binary 948,016 B (+800 B over 1.4.0). 2026-05-07
- **Prior release**: `1.4.0` — First non-trivial external plugin folded in. `[deps.cyim-lsp]` block in `cyrius.cyml` pulls cyim-lsp 1.0.2's bundled distfile (`dist/cyim-lsp.cyr`); `lib/cyim-lsp.cyr` symlink resolved by `cyrius deps` (sha in `cyrius.lock`). New `src/plugins/lsp_glue.cyr` (382 lines, adapted from cyim-lsp's `docs/examples/cyim_glue.cyr` reference) implements the consumer-side glue: buffer materialization, six hook callbacks, four `:lsp-*` ex-commands, `cyim_lsp_init()` registration. `src/main.cyr` pulls `lib/cyim-lsp.cyr` after `lib/vyakarana.cyr`, includes `src/plugins/lsp_glue.cyr` after `trailing_ws.cyr`, calls `cyim_lsp_init()` after `trailing_ws_init()`. Bundle self-containment verified: zero references to cyim-side editor symbols (`editor_*`, `buf_get`, `buf_len`, `plugin_register_*`, `DIAG_*`, `ACT_NONE`, `diag_new`) in the 2163-line distfile. Critical regression check passed: narrow tests like `tests/buffer.tcyr` continue to compile cleanly with the plugin TU folded in (this was the structural failure that blocked the original v1.0.0 fold-in attempt with cyim-lsp 1.0.0/1.0.1). User-visible features: server-pushed diagnostics → status-segment counts + inline render via `diagnostic_provider`, `:lsp-restart` / `:lsp-status` / `:lsp-goto-def` / `:lsp-find-refs`. Same-file goto-def works; cross-file deferred until cyim ships `plugin-buf-load-file` ABI. Reference quickfix list deferred until cyim ships `plugin-list-display` ABI. cyim 1.x plugin ABI freeze (1.3.6 / ADR 0004) holds. Cyrius toolchain pin unchanged (5.9.16). 2026-05-07
- **Prior release**: `1.3.7` — Closeout pass before v1.4.0 per CLAUDE.md Closeout Pass policy. All 11 audit steps passed: full clean test + fuzz from `rm -rf build`, dead-code floor recorded (24 functions: 7 plugin ABI public surface kept intentional per ADR 0004, 17 legacy helpers below the third-instance threshold), refactor pass found no parallel codepaths warranting consolidation, code review walked `1.3.0..HEAD` diff end-to-end with no findings, security re-scan found no new `sys_system` / unchecked syscalls (3 new `var buf[N]` all bounded), doc sync confirmed, version-verify across all surfaces. cyim-lsp v0.1.0 scaffolded as sibling repo `MacCracken/cyim-lsp` (sandhi pattern); v1.4.0 picks it up once cyim-lsp's v0.5.0 publishDiagnostics behaviour ships. Pure verification cut; no source changes; binary byte-identical to v1.3.6, 2026-05-06
- **Prior release**: `1.3.6` — ADR 0004 freezes the plugin ABI surface at the v1.3.5 shape. All six hook registration functions, callback signatures, 24 B diag record layout, and DIAG_* severity constants stable across cyim 1.x. Compatibility envelope: backwards-compatible additions allowed within 1.x (new hook types via ADR, new helpers, new severity values appended); breaking changes wait for cyim 2.x. Cyrius toolchain pin bumped 5.9.13 → 5.9.16 (re-vendored `lib/fs.cyr` lost `dir_walk_with_prunes` upstream — cyim doesn't consume, DCE-stripped). Pure doc + toolchain cut; no cyim source changes. Binary byte-identical to v1.3.5 (cyim doesn't consume the changed paths — DCE delivers an exact match). v1.4.0 (cyim-lsp) unblocked, 2026-05-06
- **Prior release**: `1.3.5` — Four remaining hook fire-points wired: `status_segment` (in `render_status`), `normal_key` (in `editor_dispatch`'s NORMAL arm; falls through after built-in keymap miss), `ex_command` (in `command_execute`'s parser; falls through after every built-in `:cmd` miss), `diagnostic_provider` (in `render_frame`'s top — populates `plugin_last_diags()` once per frame). Built-ins win on conflict per ADR 0003 §3. Diag record shape pinned at v1.3.5: 24 B `{line, severity, msg_ptr}` with `DIAG_HINT/INFO/WARNING/ERROR` constants + `diag_new(...)` helper. First inline plugin shipped: `src/plugins/trailing_ws.cyr` registers post_change_hook (recompute line-set), diagnostic_provider (1 DIAG_HINT per trailing-ws line), status_segment ("TWS:N", omits at N==0). 6 of 6 hooks now active. 4 more test files needed `include "src/plugin.cyr"` before `include "src/mode.cyr"` (mode.cyr now references `_plugin_lookup_normal_key`). New `tests/trailing_ws.tcyr` (24 assertions, 8 groups). `tests/plugin.tcyr` extended with dispatch integration tests (33 total, was 27). CLAUDE.md Work Loop step 3 + Closeout step 1 now call out `cyrius fuzz` (closes the v1.3.4 CI-fuzz-gap). All gates green: cyrius test, cli_smoke 118/118, integration smoke PASS, cyrius fuzz 3/3, lint clean. ABI freezes at ADR 0004 (1.3.6 plan), 2026-05-06
- **Prior release**: `1.3.4` — Plugin ABI scaffold per ADR 0003. `src/plugin.cyr` (230 lines): 6-hook registry, `plugin_init()`, register/fire/lookup/collect API. Wired `post_save` + `post_change` fire-points; other 4 hooks deferred to 1.3.5. New `tests/plugin.tcyr` (27 assertions). New `docs/architecture/001-plugin-system.md`. 8 test files + 1 fuzz file gained `include "src/plugin.cyr"`, 2026-05-06
- **Prior prior**: `1.3.3` — BUG-001 retirement cut. Cyrius pin bumped 5.9.2 → 5.9.13. Removed `_cli_args_reload_big()` workaround, 2026-05-06

## Binary

- `build/cyim` — DCE build size: **1,203,936 B** (v1.6.1 — +238,504 B over v1.6.0's 965,432 B; delta is dominated by vyakarana 2.2.1's 45 bundled grammars vs 1.0.2's narrower set — `lib/vyakarana.cyr` is now 4,036 lines. Edge: `src/lang.cyr` only routes 10 of those grammars today, so 35 of them are linked but unreachable from the editor's extension-detection path — they'll come live as 1.6.2 wires extensions. Cyrius 5.9.16 → 5.10.10 stdlib drift contributes negligibly under DCE). v1.6.0 was 965,432 B (+7,712 B over v1.5.3's 957,720 B; deltas: marks.cyr (~3.5 KB after DCE) + mode.cyr prefix-resolution branches (~2 KB) + bl_find_by_buf (~200 B) + marks_init wiring (~50 B) + linked stdlib paths the marks code reaches into (~2 KB)). v1.5.3 was 957,720 B (+1,352 B over v1.5.2's 956,368 B; deltas: F-CO-3 prefix-clear (~80 B), F-CO-4 lsp_uri_decode call sites + status messages (~400 B), cyim-lsp 1.2.1's `lsp_uri_decode` + `_lsp_hex_digit` linked in (~870 B)). v1.5.2 was 956,368 B (byte-identical to v1.5.1 modulo the `_VERSION_STR_CYIM` regen; pure closeout / verification cut). v1.5.1 was 956,368 B (+2,856 B over v1.5.0's 953,512 B; deltas: refs response parser (~1.2 KB), label builder (~700 B), on_select callback (~400 B), module globals + scaffolding (~600 B)). v1.5.0 was 953,512 B (+3,064 B over v1.4.3's 950,448 B; deltas: list-display ABI + helpers + accessors (~600 B), dispatch interception (~250 B), `_render_list_overlay` (~1.5 KB), padding / scaffolding (~700 B)). v1.4.3 was 950,448 B (+560 B over v1.4.2's 949,888 B; deltas: gd/gr handler bodies (~80 B), prefix-key registration calls (~80 B), cross-file load + materialize-and-jump path (~400 B)). v1.4.2 was 949,888 B (+1,872 B over v1.4.1's 948,016 B; deltas: prefix-key registry vec + record helper + lookup walker (~600 B), generalized prefix dispatch (~400 B), `_buf_load_file_into_active` extraction + plugin_buf_load_file wrapper (~700 B), wired ACT_MOVE_FILE_START / motion_file_start cases (~150 B). v1.4.1 was 948,016 B (+800 B over v1.4.0's 947,216 B; the delta is cyim-lsp 1.0.3's `_lsp_proc_envp_from_self` helper + `/proc/self/environ` read path, inherited via the bundled distfile. v1.4.0 was 947,216 B (v1.4.0 fold-in; +47,728 B over v1.3.7's 899,488 B = `lib/cyim-lsp.cyr` 1.0.2 distfile (~36 KB used after DCE) + `src/plugins/lsp_glue.cyr` (382 lines, ~10 KB after DCE) + a few hundred bytes of new register/dispatch wiring. cyim-lsp ships seven [lib] modules — jsonrpc, subprocess, lsp_diags, lsp_client, lsp_state, lsp_position, lsp_documents — but DCE strips unused surface aggressively; the live footprint is the goto-def + find-refs + post_change/save/diagnostic-provider chain). v1.3.7 was 899,488 B (closeout cut; byte-identical to v1.3.6 — pure verification, no source or toolchain changes). v1.3.6 was 899,488 B (ABI freeze + toolchain bump; byte-identical to v1.3.5 — the cyrius 5.9.13 → 5.9.16 stdlib paths cyim consumes are byte-equivalent after DCE). v1.3.5 was 899,488 B (plugin ABI end-to-end + trailing_ws inline plugin; +4,592 B over v1.3.4's 894,896 B — trailing_ws plugin (~700 B) + 4 fire-point integrations (~2.5 KB across render.cyr / mode.cyr / command.cyr) + diag record helpers (~600 B); all 4 previously-DCE'd register/lookup/collect helpers now have active call sites). v1.3.4 was 894,896 B (plugin ABI scaffold; +4,544 B over v1.3.3's 890,352 B — `src/plugin.cyr` 230 lines + fire-point wiring ~30 LOC; the 4 non-wired hooks' register/lookup/collect helpers are linked but DCE-stripped until a plugin uses them). v1.3.3 was 890,352 B (BUG-001 retirement + cyrius 5.9.13 pickup; −4,400 B from v1.3.2's 894,752 B — `_cli_args_reload_big()` removal recovers ~600 B; balance is downstream benefit from cyrius 5.9.3-5.9.13 stdlib improvements consumed by cyim's regex/niyama paths). v1.3.2 was 894,752 B (fuzzy precision + `--fuzzy-edits` wiring + closeout; +5,624 B over v1.3.1's 889,128 B — `_fuzzy_span_end` helper, `_regex_opts_set_fuzzy_edits`, `--fuzzy-edits=` parser arm × 6 dispatchers, threading through 4 `run_*` functions, expanded `--help` flavor table). v1.3.1 was 889,128 B (fuzzy wiring; +720 B over v1.3.0's 888,408 B — fuzzy compile arm + cstring-offset pseudo-`_search_at` + plen-approximation `_group_end` inline at two call sites + expanded `--help` flavor table; niyama_fuzzy + unicode tables already linked in v1.3.0 so no engine-level expansion). v1.3.0 was 888,408 B (v1.3.0 niyama fold + 4 engine flavors; +518,640 B over v1.2.2's 369,768 B = niyama dist (~6.6 KLOC of engine code: bre/re2/pcre/fuzzy/vim engines + posix_classes + unicode_props shared shims) plus the unicode normalization tables (`lib/unicode/_normalize_data.cyr` is ~300 KB on its own). The flat 4-engine dispatch in `src/cli.cyr` (FLAVOR_BRE/RE2/PCRE/VIM constants, `_re_search_at` / `_re_search` / `_re_group_end` helpers, `_flavor_validate` parser arm) adds <2 KB. v1.2.2 was 369,768 B (toolchain bump 5.7.23 → 5.9.1 + version-string cleanup; +13,344 B over v1.2.1's 356,424 B = +13,264 B stdlib drift between the two cyrius releases plus +80 B for the `src/version_str.cyr` extraction (single-source-of-truth for `cyim --version`, regenerated by `scripts/version-bump.sh` per the cyrius pattern). v1.2.1 added `editor_feed` + 8-byte read buffer in `run_editor`/`run_headless` for driver-side CSI parsing, +1,168 B over v1.2.0's 355,256 B; v1.2.0 added the Matcher + RegexOpts abstractions, `_cli_count_matches_m` / `_cli_substitute_regex` / `_cli_substitute_m` matcher-dispatching helpers, six `_dispatch_<verb>(ac)` extraction functions, and the cyrius stdlib `regex` dep — Pike NFA engine consumed via `regex_compile`/`regex_search`/`regex_search_at`/`regex_group_*`. The bulk of the +44,336 B delta from 1.1.4 is the engine itself; cyim consumer code adds ~4 KB; the dispatch extraction is byte-neutral. v1.1.4 was 312,088 B; v1.1.3 was 300,640 B with 8 duplicate-flag guards across four verbs in `src/main.cyr` + a slightly longer `--grep` help string; v1.1.2 was 298,392 B with `run_batch` + `_cli_drain_stdin_raw` in `src/cli.cyr` and `--batch` dispatch in `src/main.cyr`; v1.1.0 was 293,192 B; v1.0.2 was 283,984 B; v1.0.0 was 274,656 B; M6 was 273,912 B; M5 was 262,504 B; M4 was 256,344 B; M3 was 226,064 B; M2 was 162,184 B; M1 was 101,560 B; M0 stub was 57,728 B).

## Tests

- Test suites: M0 + M1 (8) + M2 (4) + M3 (2) + M4 (4)
- Assertion count: 847 (M1 350; M2 +117 = 467; M3 +192 = 659; M4 +153 = 812; M6 +26 = 838; M7 +9 = 847)
- Integration smoke: `tests/integration_smoke.py` — 45 PASS assertions across PTY-driven + headless-subprocess sections: `--headless`, `--write`, `--replace`, `--replace-all`, `--grep`, `--write --expect[/-not]`, `--replace[-all] --expect-N/--expect-1`, multi-window cascade
- CLI parser smoke: `tests/cli_smoke.sh` — 118 PASS assertions across 94 cases (v1.3.2 added 4 fuzzy cases — `--fuzzy-edits=0` exact-only, `--fuzzy-edits` rejected without `--regex=fuzzy`, duplicate `--fuzzy-edits` rejected; case 87 updated to assert the v1.3.2 tight-span behavior). v1.3.1 added 6 fuzzy cases. v1.3.0 added 10 cases for the four niyama flavors. (10 v1.1.1 interspersed-modifier regressions + 18 v1.1.2 `--batch` cases incl. atomicity, malformed-stdin, post-save assertions, em-dash unicode round-trip + 7 v1.1.3 cases (6 duplicate-flag refusals across the four verbs + a `--grep '^foo'` literal-substring regression) + 17 v1.1.4 cases: 5 `--grepfiles` (single/multi-file match, no-match exit 1, missing FILE exit 3, FILE:N:LINE shape), 6 `--context=N` (pre/match/post emit shape, overlapping-window merge, non-adjacent `--` separator, `--context=0` byte-for-byte back-compat regression guard, `--context=<non-int>` rejection, duplicate `--context=` refusal), and 6 `--replace-files[-all]` (multi-line OLD/NEW round-trip, empty OLD_FILE rejection, missing OLD_FILE exit 3, non-unique OLD exit 5, atomicity guard on `--expect-1` mismatch, `--wc=l` output shape) + 16 v1.2.0 cases: `--regex=ere` per-verb basics (digits / alternation / anchors), literal-default back-compat regression guard, error paths (invalid pattern / unknown flavor / empty flavor / duplicate `--regex=`), multi-file regex via `--grepfiles`, regex × file-sourced OLD/NEW via `--replace-files[-all]`, and three composition cases (`--regex × --context`, `--regex × --wc=l`, `--regex × --expect-1`))
- Fuzz harnesses: 3; all pass under `cyrius fuzz`
- Performance benches: 9; M5 baseline + M6 cache-hit win in [`BENCHMARKS.md`](../../BENCHMARKS.md)
- Security audit: initial pass [M5](../audit/2026-04-25-security-audit.md), second pass [M7](../audit/2026-04-25-m7-audit.md), 0day-corpus survey at [`docs/security/2026-04-25-0day-corpus.md`](../security/2026-04-25-0day-corpus.md), trust-model ADR at [`docs/adr/0001-trust-model.md`](../adr/0001-trust-model.md). **End-of-M7 triage: 0 CRITICAL / 0 HIGH / 0 MEDIUM**; 8 LOW findings all triaged with rationale.
- Cleanliness: `cyrius lint` **0 warnings** (down from 42 pre-v1.2.0 cleanup — see CHANGELOG § Internal); `cyrius fmt --check` clean across all `src/*.cyr`

## Active Milestone

- **M0** — scaffold. *Done.*
- **M1** — gap-buffer + raw-mode TTY + modal dispatch. *Done.*
- **M2** — syntax highlighting via `vyakarana`. *Done.*
- **M3** — multi-buffer + splits + window navigation. *Done.*
- **M4** — search, undo, visual, `.` repeat, `:set` + `.cyimrc`. *Done.*
- **M5** — polish: docs, perf benchmarks, fuzz, receipts. *Done.*
- **M6** — P(-1) hardening: tokenbuf cache, F-1/F-3/F-4 closures, cleanliness gate, refactor pass. *Done.*
- **M7** — Security audit: 0day corpus, checklist re-walk, F-2 fix, trust-model ADR, M7.5 verification pass. *Done.* All CVE references in the corpus survey verified against primary sources.
- **v1.0** — release. *Done — 2026-04-25.*
- **v1.0.1** — agent-drive CLI surface: `--write`, `--replace`, `--replace-all` folded into the binary directly (src/cli.cyr). *Done — 2026-04-25.*
- **v1.0.2** — `--wc` modifier on agent-drive ops + BUG-001 fix (silent argv truncation > 4 KB). *Done — 2026-04-26.*
- **v1.1.0** — `--grep` (read-only line scan), `--expect`/`--expect-not` (post-write shape assertion on `--write`), `--expect-N`/`--expect-1` (pre-substitution count assertion on `--replace[-all]`). Closes the "tool boundary jump" — structural-invariant checks stay in one binary. *Done — 2026-04-26.*
- **v1.1.1** — agent-drive CLI flag-parser fix: modifiers (`--wc`, `--expect[-not]`, `--expect-N`, `--expect-1`) now parse in any position after the verb, including interleaved with or after positionals. *Done — 2026-04-26.*
- **v1.1.2** — `--batch` agent-drive verb: N substitutions in one call from a NUL-separated stdin stream (`OLD1\0NEW1\0OLD2\0NEW2\0…\0`). Atomic — failure mid-batch leaves the file untouched. Closes the cyim pain point in cyrius-bb's tooling field notes. Cyrius toolchain pin bumped 5.7.1 → 5.7.7. *Done — 2026-04-26.*
- **v1.1.3** — agent-drive paper cuts: duplicate modifier flags (`--expect[-not]=`, `--expect-N=`/`--expect-1`, `--wc[=…]`, `--all`) now refused with exit 2 across all four verbs (was: silent last-wins). `cyim --help` for `--grep` now states "literal substring, not regex". Surfaced in the dogfood loop as "two cyim observations to confirm next sweep". *Done — 2026-04-26.*
- **v1.1.4** — grep-surface expansion (split out of the regex bundle ahead of the upstream-gated `--regex=<flavor>`) + dogfood-driven `--replace-files[-all]`: `cyim --grepfiles <pattern> <file...>` (multi-file grep, `FILE:N:LINE` per match, exit 0/1/2/3 per `grep` convention), `--context=<n>` modifier on `--grep`/`--grepfiles` (`grep -C N` shape with overlapping-window merge + `--` separators between non-adjacent groups and between files), `cyim --replace-files OLD_FILE NEW_FILE FILE` (and `--replace-files-all`) reading OLD/NEW from file contents instead of argv (closes shell-escape friction surfaced when splicing multi-line edits via `--batch`). Cyrius toolchain pin bumped 5.7.7 → 5.7.13 (5.7.13's `lib/regex.cyr` is glob-only — *not* the planned NFA ABI — so `--regex=<flavor>` stays gated; roadmap escalates that piece to a hard pre-cyrius-5.7.x-EOL target). *Done — 2026-04-27.*
- **v1.2.0** — `--regex=<flavor>` modifier on the four pattern verbs (`--grep`, `--grepfiles`, `--replace`, `--replace-all`) and the two file-sourced variants (`--replace-files`, `--replace-files-all`). Today's only valid flavor: `ere` (cyrius stdlib `lib/regex.cyr` Pike NFA — POSIX-ERE-ish). Default stays literal substring (back-compat regression-guarded by `cli_smoke.sh` case 62). Internal threading uses a `RegexOpts` struct with reserved 8-byte slots so future per-engine options (icase, multiline, dotall, ungreedy) extend without rewrite. ADR 0002 records the extensibility shape. Cyrius toolchain pin bumped 5.7.13 → 5.7.23 — first release exposing the Pike NFA engine (landed v5.7.18) through the `regex_*` ABI. Six `_dispatch_<verb>(ac)` extraction functions added in `src/cli.cyr` to keep `main()` under Cyrius's per-function 64-return cap. Additional engines (`bre`, `re2`, `pcre`, `fuzzy`, `vim`) ship via the niyama standalone Cyrius lib; cyim's parser-side picks them up via one extra `elif` arm per flavor. *Done — 2026-04-28.*

## Post-v1.0

Demand-gated work now continues per [`roadmap.md`](roadmap.md)'s
post-v1.0 table. v1.0.1 closed the agent-drive ergonomics gap
(`--headless` + `--write` + `--replace` + `--replace-all` all
ship in the binary). v1.1.0 closes the structural-invariant
gap — `--grep` + `--expect[-not]` + `--expect-N`/`--expect-1`
let scripts assert "after this rewrite, X must / must not appear"
or "this substitution must hit exactly N times" without leaving
the binary. Remaining: system clipboard (via `aethersafha`),
LSP client (when cyrius-lsp stabilizes), terminal embed,
macros, etc.

## Consumers

| Consumer | Status | Notes |
|----------|--------|-------|
| `agnoshi` | Planned | Wires at M3 (multi-buffer ready) |
| `aethersafha` | Planned | Wires at M3 (Wayland terminal hosts cyim) |
| `daimon`-orchestrated agents | Surface ready (v1.2.0) | `cyim --headless` for full keymap drive; `cyim --write`/`--replace`/`--replace-all`/`--replace-files`/`--replace-files-all`/`--grep`/`--grepfiles`/`--batch` for one-shot ops; `--expect[-not]`, `--expect-N`/`--expect-1`, `--all` (on `--batch`), `--context=<n>` (on `--grep`/`--grepfiles`), and `--regex=<flavor>` (on the four pattern verbs + the two file-sourced variants — `ere` flavor today via cyrius stdlib Pike NFA; future flavors via niyama) modifiers compose orthogonally. `--replace-files[-all]` reads OLD/NEW from file paths so multi-line edits and regex patterns don't need argv escaping. Duplicate modifier flags refused with exit 2 (v1.1.3+); the v1.1.4 `--context=` parser machinery shape was the template for v1.2.0's `--regex=` threading. Daimon integration when that consumer is ready. |

## Dependencies

- **stdlib (auto-prepend)**: `syscalls`, `alloc`, `fmt`, `io`, `fs`, `str`, `string`, `vec`, `args`, `hashmap`, `assert`, `bench`, `regex` (Pike NFA / POSIX-ERE-ish for `--regex=ere`, v1.2.0), `unicode/_decode`, `unicode/categories`, `unicode/_categories_data`, `unicode/casefold`, `unicode/_casefold_data`, `unicode/normalize`, `unicode/_normalize_data` (added v1.3.0 — required by niyama_fuzzy's NFD path; per cyrius v5.8.49 subdir-nested-stdlib resolution).
- **stdlib (sandhi-pattern, explicit-include)**: `lib/niyama.cyr` (folded byte-identical at cyrius 5.9.0 from niyama 1.0.1 dist; pulled via `include "lib/niyama.cyr"` in `src/main.cyr` rather than `[deps].stdlib` auto-prepend, to keep preprocess_out under the 2 MB cap that motivated cyrius's opt-in fold pattern at v5.8.65). Provides `niyama_{bre,re2,pcre,vim}_*` ABI consumed by v1.3.0; `niyama_fuzzy_*` linked but parser-gated to v1.3.1.
- **External Cyrius deps**: `vyakarana` (2.2.1 as of 1.6.1 — bumped from 1.0.2 across 14 minor cuts; only break in the window is 2.0.0's `tokenize_source` → streaming-primitive replacement, which `src/highlight.cyr` consumes in 5 lines + null-handle guard. Distfile now ships 45 grammars; cyim's `src/lang.cyr` routes the original 10 — full extension catch-up is the 1.6.2 bite); `cyim-lsp` (1.2.1 as of 1.5.3 — first real bundle source change since 1.0.3 added `lsp_uri_decode` for percent-encoded `file://` URIs; consumer-side glue activates prefix-keymap + buf-load-file (1.1.0/cyim 1.4.3) + plugin_list_display refs quickfix (1.2.0/cyim 1.5.1) + URL-decode (1.2.1/cyim 1.5.3); 1.6.1 moved cyim-lsp's own `[package].cyrius` pin 5.9.16 → 5.10.10 in lockstep, no tag bump).

## Verification Hosts

- x86_64 Linux (primary dev) — verified at scaffold
- aarch64 Pi — pending M1 hardware test
- Apple Silicon Mach-O — pending M1 hardware test
- Windows PE32+ — out of scope (TTY editor; Linux/macOS/BSD targets only)

## Bootstrap Chain

`cyrius` (5.10.10 as of 1.6.1; was 5.9.16 across 1.3.6–1.6.0) → vendored stdlib in `lib/` (includes `regex.cyr` + `niyama.cyr` from 5.9.0+ fold + `unicode/*.cyr` properly packaged from 5.9.2; `args.cyr` heap-backed since 5.9.5; `fs.cyr` dir_list UAF fix in 5.9.6; 5.10.x window adds SIMD primitives, multi-stack `#derive(...)` directives, JSON RFC 8259 §7 escape compliance, return-cap raise, TLS plumbing — none of which cyim's surface consumes directly, but vyakarana 2.x and stdlib drift inherit them) → `src/main.cyr` (explicit `include "lib/niyama.cyr"` per sandhi pattern) → `build/cyim`.
Zero external dependencies as of M0.
